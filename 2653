#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *joias[1000005];
char buffer[1000005];

int compare(const void *a, const void *b) {
    return strcmp(*(const char **)a, *(const char **)b);
}

int main() {
    int n = 0;

    while (scanf("%s", buffer) != EOF) {
        joias[n] = (char *)malloc(strlen(buffer) + 1);
        strcpy(joias[n], buffer);
        n++;
    }

    if (n == 0) {
        printf("0\n");
        return 0;
    }

    qsort(joias, n, sizeof(char *), compare);

    int count = 1;
    for (int i = 1; i < n; i++) {
        if (strcmp(joias[i], joias[i-1]) != 0) {
            count++;
        }
    }

    printf("%d\n", count);

    return 0;
}
