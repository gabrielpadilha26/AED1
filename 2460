#include <stdio.h>

int main() {
    int N, M, x;
    int removed[100001] = {0};
    int fila[50000];

    scanf("%d", &N);
    for (int i = 0; i < N; i++)
        scanf("%d", &fila[i]);

    scanf("%d", &M);
    for (int i = 0; i < M; i++) {
        scanf("%d", &x);
        removed[x] = 1;
    }

    int first = 1;
    for (int i = 0; i < N; i++) {
        if (!removed[fila[i]]) {
            if (first) {
                printf("%d", fila[i]);
                first = 0;
            } else {
                printf(" %d", fila[i]);
            }
        }
    }
    printf("\n");

    return 0;
}
