#include <stdio.h>

int main() {
    int X, Z;
    int sum = 0, count = 0;

    scanf("%d", &X);

    do {
        scanf("%d", &Z);
    } while (Z <= X);

    while (sum <= Z) {
        sum += X;
        count++;
        X++;
    }

    printf("%d\n", count);

    return 0;
}
