#include <stdio.h>

int main() {
    int NC, N, h;
    int freq[231];
    scanf("%d", &NC);

    while (NC--) {
        scanf("%d", &N);

        for (int i = 20; i <= 230; i++)
            freq[i] = 0;

        for (int i = 0; i < N; i++) {
            scanf("%d", &h);
            freq[h]++;
        }

        int first = 1;
        for (int i = 20; i <= 230; i++) {
            while (freq[i]--) {
                if (first) {
                    printf("%d", i);
                    first = 0;
                } else {
                    printf(" %d", i);
                }
            }
        }
        printf("\n");
    }

    return 0;
}
